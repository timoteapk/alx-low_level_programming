#include <stdio.h>
/**
 * main - prints all possible combination of single digits
 *
 * Return: always 0 sucessful
 */
int main(void)
{
	int i, j;

	for (int i = '0'; i <= '9'; i++)
	{
		for (j = i + '1'; p <= '9'; j++)

	return 0;
}
